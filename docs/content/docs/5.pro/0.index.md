---
title: Introduction
description: Advanced components for professional applications.
navigation:
  title: Introduction
  badge:
    label: Pro
    color: primary
---

## What is Nuxt UI Elements Pro?

**Nuxt UI Elements Pro** extends the free [Nuxt UI Elements](/docs/getting-started) module with advanced, feature-rich components built for professional applications.

Pro components follow the same design principles as the free library — full TypeScript support, auto-imports, Nuxt UI integration, and dark mode — while providing more complex functionality like calendars, advanced data displays, and more.

## Installation

Install the pro package alongside the free module:

```bash
pnpm add nuxt-ui-elements-pro
```

Add it to your `nuxt.config.ts`:

```ts
export default defineNuxtConfig({
  modules: [
    'nuxt-ui-elements',
    'nuxt-ui-elements-pro'
  ]
})
```

## License

Nuxt UI Elements Pro requires a license key for production builds. Set it via environment variable:

```bash
NUXT_UI_ELEMENTS_PRO_KEY=your-license-key
```

Or in your `nuxt.config.ts`:

```ts
export default defineNuxtConfig({
  uiElementsPro: {
    license: process.env.NUXT_UI_ELEMENTS_PRO_KEY
  }
})
```

During development, the module works without a license key (a warning is logged).

## Configuration

The pro module accepts the same `prefix` option as the free module:

```ts
export default defineNuxtConfig({
  uiElementsPro: {
    prefix: 'UE', // Default. Components: UEEventCalendar, etc.
    license: process.env.NUXT_UI_ELEMENTS_PRO_KEY
  }
})
```

## Pro Components

| Component | Description |
| --- | --- |
| [EventCalendar](/docs/pro/event-calendar) | A full-featured calendar with month, week, and day views, drag-and-drop, and keyboard navigation |
